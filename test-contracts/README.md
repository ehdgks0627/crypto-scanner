# Test Contracts

This directory contains test contracts that turn the project plan and API
contract into executable test intent.

- `api/scenarios.md`: natural-language backend API acceptance scenarios.

These files are intentionally separate from `docs/`, which contains the initial
project plan and product/API design documents.

PQC 전환 자동화 시스템 - 최종 시연 구현 TODO
기준 문서: 작업지시서.txt
최근 갱신: 2026-05-17

상태 표기:
  [ ] 미구현
  [~] 일부 구현됨, 시연 요구사항에 맞게 보완 필요
  [x] 완료

원칙:
  - 발표자는 웹 대시보드만 조작한다.
  - 각 단계는 "다음" 버튼으로 진행된다.
  - 백엔드는 다음 단계에 필요한 작업을 자동으로 실행한다.
  - 시연 결과는 매번 같은 숫자로 재현되어야 한다.
  - 작업을 끝낼 때마다 이 파일을 갱신하고 커밋한다.


===============================================================================
0. 시연 전체 목표
===============================================================================

[ ] 0.1 대시보드에서 6단계 시연 흐름을 한 번에 진행할 수 있게 만든다.
        단계:
        1) 대상 등록
        2) Discovery Agent + Host Agent 실행
        3) Enriched CBOM 확인
        4) AI 위험도 평가
        5) PQC 매핑 추천
        6) 가용성 검증

[ ] 0.2 한 번의 시연 사이클이 5분 이내에 끝나게 만든다.

[ ] 0.3 동일 입력에서 동일 결과가 나오도록 시연 데이터를 결정론적으로 고정한다.

[ ] 0.4 시연 중 네트워크나 LLM 장애가 있어도 발표 흐름이 깨지지 않도록 fallback 결과를 제공한다.


===============================================================================
1. 시연 데이터 및 리셋 스크립트
===============================================================================

[ ] 1.1 `./demo-reset.sh`를 추가한다.
        완료 기준:
        - 기존 DB 시연 데이터를 삭제한다.
        - migration을 적용한다.
        - 시연용 target, label, asset, snapshot, job 데이터를 다시 주입한다.
        - 실행 후 대시보드가 STEP 01 초기 상태로 열린다.

[ ] 1.2 `./seed-labels.sh`를 추가한다.
        완료 기준:
        - 핵심 호스트 라벨이 DB에 들어간다.
        - `srv-01` 라벨이 아래 값으로 고정된다.
          role: edge-proxy
          data_classes: PII, payment
          partners: PG-A
          retention: 7y

[ ] 1.3 `./record-demo.sh`를 추가한다.
        완료 기준:
        - 발표용 백업 영상을 남길 수 있다.
        - 로컬 또는 배포 URL을 대상으로 녹화할 수 있다.

[ ] 1.4 시연 대상 13개를 seed로 고정한다.
        완료 기준:
        - 10.0.0.0/24
        - payments.demo.local
        - ssh.demo.local
        - mail.demo.local
        - 13개 서비스 클러스터 전체가 커버된다.

[ ] 1.5 시연 자산 수를 정확히 47개로 맞춘다.
        현재 상태:
        - 현재 `LATEST_ASSETS`는 67개로 작업지시서와 불일치한다.
        완료 기준:
        - 총 자산 47개
        - Discovery Agent 발견 28개
        - Host Agent 발견 24개
        - 양쪽 모두에서 발견된 중복 5개
        - 합집합 47개

[ ] 1.6 알고리즘 분포를 발표 수치와 맞춘다.
        완료 기준:
        - RSA 14개
        - ECDSA 6개
        - Ed25519 5개
        - DH/X25519 8개
        - AES/ChaCha20 9개
        - SHA256/SHA512 5개

[ ] 1.7 잠든 키 파일 수를 3개로 고정한다.
        완료 기준:
        - 활성 자산 44개
        - 사용되지 않는 키 파일 3개
        - UI에서 별도 표시된다.

[ ] 1.8 위험도 분포를 발표 수치와 맞춘다.
        현재 상태:
        - 현재 seed는 CRITICAL/HIGH/MEDIUM/LOW 기반이라 P1/P2/P3 발표 수치와 다르다.
        완료 기준:
        - P1 12개
        - P2 8개
        - P3 27개

[ ] 1.9 핵심 예시 자산 `srv-01:443/tls`를 고정한다.
        완료 기준:
        - algorithm: RSA-2048
        - role: edge-proxy
        - data_classes: PII, payment
        - partners: PG-A
        - retention: 7y
        - AI 위험도 결과: 9.2점 / P1


===============================================================================
2. 백엔드 시연 오케스트레이션
===============================================================================

[ ] 2.1 시연 흐름 상태를 저장하는 모델 또는 서비스 레이어를 추가한다.
        완료 기준:
        - 현재 단계
        - 단계별 상태
        - 연결된 target, discovery, scan, snapshot, assessment, migration, verification id
        - 마지막 오류 메시지
        - 재실행 가능 여부

[ ] 2.2 시연용 API를 추가한다.
        권장 API:
        - POST /api/demo/reset
        - GET  /api/demo/session
        - POST /api/demo/session/start
        - POST /api/demo/session/next
        - GET  /api/demo/session/events

[ ] 2.3 기존 API와 작업지시서 API의 차이를 정리한다.
        현재 상태:
        - 실제 API는 `/discoveries`, `/snapshots`, `/performance-runs` 중심이다.
        - 작업지시서는 `/api/scans`, `/api/cbom`, `/api/assessments`, `/api/verifications`를 기대한다.
        완료 기준:
        - 기존 API를 유지하면서 시연용 wrapper API를 제공하거나,
        - 프론트가 현재 API에 맞춰 6단계 흐름을 구성한다.

[ ] 2.4 각 단계의 비동기 작업을 자동 연결한다.
        완료 기준:
        - STEP 01 완료 후 STEP 02 실행 가능
        - STEP 02 완료 후 snapshot 생성
        - STEP 03 완료 후 assessment 실행
        - STEP 04 완료 후 migration plan 생성
        - STEP 05 완료 후 verification 실행

[ ] 2.5 진행 상황 스트림을 제공한다.
        완료 기준:
        - WebSocket 또는 SSE
        - 어려우면 짧은 주기 polling으로 대체
        - UI에서 로그와 진행률이 자연스럽게 증가한다.


===============================================================================
3. STEP 01 - 대상 등록
===============================================================================

[ ] 3.1 시연 모드의 대상 등록 화면을 만든다.
        완료 기준:
        - IP / Domain / CIDR 입력
        - 대상 추가 버튼
        - 등록된 대상 테이블
        - 13개 seed target이 보인다.

[ ] 3.2 호스트 라벨링 UI를 만든다.
        완료 기준:
        - role 드롭다운
        - data_classes 체크박스
        - partners 태그 입력
        - retention 드롭다운
        - `srv-01` 라벨이 seed 값으로 표시된다.

[ ] 3.3 STEP 01 완료 조건을 구현한다.
        완료 기준:
        - target 13개 이상 존재
        - `srv-01` 필수 라벨 존재
        - 조건 충족 시 "다음 단계" 버튼 활성화


===============================================================================
4. STEP 02 - Discovery Agent + Host Agent 실행
===============================================================================

[~] 4.1 Discovery Agent 결과를 시연 수치에 맞게 정리한다.
        완료 기준:
        - Discovery Agent 발견 자산 28개
        - TLS, STARTTLS, SSH, IKE 진행 로그 표시
        - 인증서 체인 수집 로그 표시

[~] 4.2 Host Agent 결과를 시연 수치에 맞게 정리한다.
        완료 기준:
        - Host Agent 발견 자산 24개
        - trust store, 인증서 파일, 키 파일, 설정 파일 수집 로그 표시
        - 잠든 키 파일 3개 표시

[ ] 4.3 Discovery Agent와 Host Agent 결과를 병합한다.
        완료 기준:
        - 양쪽 모두에서 발견된 중복 5개
        - fingerprint 또는 stable id 기준 dedupe
        - 최종 합집합 47개

[ ] 4.4 STEP 02 화면을 만든다.
        완료 기준:
        - 좌측 Discovery Agent 로그
        - 우측 Host Agent 로그
        - 상단 진행률 0% -> 100%
        - 발견 자산 수 0 -> 47 카운트업
        - 완료 후 "다음 단계" 활성화


===============================================================================
5. STEP 03 - Enriched CBOM 확인
===============================================================================

[~] 5.1 CBOM 자산 테이블을 시연 요구 컬럼으로 정리한다.
        완료 기준:
        - ID
        - 알고리즘
        - 키 길이
        - 만료일
        - 도메인
        - 역할
        - 연결 대상
        - 취급 정보
        - 보존 기간

[ ] 5.2 표준 필드 / 확장 필드 토글을 추가한다.
        완료 기준:
        - 표준 모드에서는 표준 CBOM 필드 중심 표시
        - 확장 모드에서는 role, neighbors, data_tags, retention 등 표시
        - 토글 전환 시 테이블과 JSON 패널이 함께 바뀐다.

[ ] 5.3 `srv-01:443/tls` 상세 JSON을 발표 예시와 맞춘다.
        완료 기준:
        - bom-ref: srv-01:443/tls
        - algorithm: RSA
        - key_size: 2048
        - host_role: edge-proxy
        - data_tags: PII, payment
        - retention_policy: 7y
        - discovered_by: discovery_agent, host_agent

[ ] 5.4 CBOM export를 시연 흐름에서 제공한다.
        완료 기준:
        - JSON 다운로드 가능
        - enriched mode 포함 가능


===============================================================================
6. STEP 04 - AI 위험도 평가
===============================================================================

[~] 6.1 DHS 6기준 명칭과 출력 구조를 작업지시서에 맞춘다.
        완료 기준:
        - value
        - data
        - scope
        - sharing
        - critical
        - lifetime

[ ] 6.2 LLM 출력 스키마를 고정한다.
        완료 기준:
        - 각 기준별 level: HIGH / MED / LOW
        - 각 기준별 reason 1문장
        - 최종 score
        - priority P1/P2/P3
        - JSON 형식 검증

[ ] 6.3 점수 산술을 코드에서 결정론적으로 수행한다.
        완료 기준:
        - HIGH/MED/LOW 판단은 LLM 또는 fallback이 제공
        - 점수 계산은 코드가 수행
        - temperature=0
        - 가능한 provider에서는 seed=42 사용
        - provider 실패 시 seed fixture로 fallback

[ ] 6.4 DHS 가중치를 작업지시서 의도에 맞게 정리한다.
        완료 기준:
        - lifetime이 가장 큰 가중치
        - `srv-01:443/tls`가 9.2점/P1로 재현
        - 전체 분포 P1 12 / P2 8 / P3 27 재현

[ ] 6.5 STEP 04 화면을 만든다.
        완료 기준:
        - 0/47 -> 47/47 진행률
        - 행별 spinner -> 점수 표시
        - 상단 요약 P1 12 / P2 8 / P3 27
        - 행 클릭 시 LLM 응답 JSON 표시


===============================================================================
7. STEP 05 - PQC 매핑 추천
===============================================================================

[~] 7.1 기존 migration mapping rules를 시연 추천 결과에 맞게 정리한다.
        완료 기준:
        - RSA 키 교환 계열 -> ML-KEM-768
        - RSA/ECDSA 서명 계열 -> ML-DSA-65
        - 장기 보존 서명 -> SLH-DSA 계열
        - 대칭키/해시는 전환 대상에서 제외 또는 감시 대상으로 표시

[ ] 7.2 추천 항목 수를 20개로 고정한다.
        완료 기준:
        - P1 12개
        - P2 8개
        - P3는 자동 전환 추천에서 제외

[ ] 7.3 추천 사유를 발표용 문장으로 정리한다.
        완료 기준:
        - 왜 전환 대상인지
        - 어떤 PQC 알고리즘으로 매핑되는지
        - 자동 변경이 아니라 추천/계획 단계임을 명확히 표시

[ ] 7.4 STEP 05 화면을 만든다.
        완료 기준:
        - 추천 대상 20개 테이블
        - 기존 알고리즘
        - 추천 PQC 알고리즘
        - 우선순위
        - 사유
        - export 가능


===============================================================================
8. STEP 06 - 가용성 검증
===============================================================================

[~] 8.1 기존 가용성 검사 기능을 시연 검증 단계에 맞게 정리한다.
        완료 기준:
        - 기능 검증
        - 성능 검증
        - 호환성 검증
        - 회귀 검증

[ ] 8.2 발표용 전후 검증 수치를 고정한다.
        완료 기준:
        - TLS handshake success: 100%
        - latency: 42ms -> 54ms
        - throughput: 2400 req/s -> 2380 req/s
        - compatibility: 100% -> 98%
        - failure count: 0
        - CBOM 변경 12개

[ ] 8.3 결과 문구를 "성능평가"가 아니라 "가용성 검증"으로 통일한다.
        완료 기준:
        - 화면
        - API schema
        - 문서
        - 테스트명
        - 버튼/카드/그래프 라벨

[ ] 8.4 STEP 06 화면을 만든다.
        완료 기준:
        - 상단 종합 결과
        - 4개 검증 카드
        - 전후 비교 그래프
        - 실패 경로 0건 표시
        - 최종 리포트 export 가능


===============================================================================
9. 프론트엔드 시연 UI
===============================================================================

[ ] 9.1 `/demo` 또는 대시보드 내 시연 모드를 추가한다.
        완료 기준:
        - 기존 운영 화면과 충돌하지 않는다.
        - 발표자는 한 화면에서 전체 흐름을 진행할 수 있다.

[ ] 9.2 단계 진행 컴포넌트를 만든다.
        완료 기준:
        - 6단계 stepper
        - 현재 단계 강조
        - 완료/진행/실패 상태 표시
        - "다음 단계" 버튼
        - "처음으로 리셋" 버튼

[ ] 9.3 단계별 데이터 로딩과 상태 갱신을 구현한다.
        완료 기준:
        - polling 또는 event stream
        - 중복 클릭 방지
        - 실패 시 재시도 버튼
        - 단계별 완료 조건 충족 전에는 다음 단계 비활성화

[ ] 9.4 발표용 텍스트를 정리한다.
        완료 기준:
        - 비전공자에게 보여줄 화면 문구는 짧고 명확해야 한다.
        - 기술 설명은 화면에 길게 쓰지 않는다.
        - 핵심 수치와 결과 중심으로 보여준다.


===============================================================================
10. 테스트 및 검증
===============================================================================

[ ] 10.1 seed 데이터 검증 테스트를 추가한다.
        완료 기준:
        - 총 자산 47개
        - Discovery 28개
        - Host 24개
        - overlap 5개
        - dormant key 3개
        - P1/P2/P3 분포 일치

[ ] 10.2 `srv-01:443/tls` 위험도 재현 테스트를 추가한다.
        완료 기준:
        - 9.2점
        - P1
        - DHS 6기준 JSON 포함

[ ] 10.3 migration 추천 결과 테스트를 추가한다.
        완료 기준:
        - 추천 20개
        - P1/P2만 추천
        - RSA/ECDSA/DH 용도별 매핑 정확

[ ] 10.4 가용성 검증 결과 테스트를 추가한다.
        완료 기준:
        - 발표 수치 고정
        - 실패 0건
        - CBOM 변경 12개

[ ] 10.5 프론트 시연 흐름 테스트를 추가한다.
        완료 기준:
        - demo route 렌더링
        - 다음 단계 버튼 동작
        - 각 단계 완료 상태 표시

[ ] 10.6 최종 빌드와 배포 확인을 수행한다.
        완료 기준:
        - backend test 통과
        - frontend test 통과
        - frontend build 통과
        - 배포 URL에서 최신 번들 반영 확인


===============================================================================
11. 배포 및 발표 준비
===============================================================================

[ ] 11.1 배포 자동화 절차를 정리한다.
        완료 기준:
        - push 후 배포 명령 또는 자동 배포 흐름 명확화
        - 배포 서버에서 migration 적용
        - demo reset 실행 가능

[ ] 11.2 발표 전 점검 명령을 문서화한다.
        완료 기준:
        - DB 리셋
        - seed 주입
        - 서비스 상태 확인
        - 대시보드 접속 확인
        - 녹화 백업 생성

[ ] 11.3 최종 발표 시연 체크리스트를 추가한다.
        완료 기준:
        - 시연 시작 URL
        - 로그인 또는 인증 필요 여부
        - 첫 화면 상태
        - 예상 소요 시간
        - 장애 시 대체 영상 위치
